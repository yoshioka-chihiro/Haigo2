配合に原料を登録！

<%= form_with model: @recipe_material, url: recipe_materials_path, method: :post do |f| %>

<%= f.label :material_id, '原料名' %>
<%= f.collection_select :material_id, Material.all, :id, :name, include_blank: "選択してください", class: "col-lg-4" %>

<%= f.label :amount, '配合量' %>
<%= f.text_field :amount %>


<%= f.submit '作成' %>

<% end %>