配合を立てる！

<%= form_with model: @recipe, local:true do |f| %>


<%= f.label :wholesale_id, '卸先' %>
<%#= f.select :, Wholesale.names_i18n.keys.map{|k| [I18n.t("enums.wholesale.name.#{k}"), k]}, {prompt: '選択してください'}, class: "form-control col-lg-5"  %>

<%= f.label :end_user_id, '最終ユーザー' %>
<%#= f.select :name, EndUser.names_i18n.keys.map{|k| [I18n.t("enums.end_user.name.#{k}"), k]}, {prompt: '選択してください'}, class: "form-control col-lg-5"  %>


<%= label :manufacturing_cost, '製造費' %>
<%= f.text_field :manufacturing_cost %>

<%= label :for_use, '用途' %>
<%= f.text_field :for_use %>

<%= label :name, '製品名' %>
<%= f.text_field :name %>


<%= f.submit '作成' %>

<% end %>